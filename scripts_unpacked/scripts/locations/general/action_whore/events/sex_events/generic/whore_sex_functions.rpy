init python:

    def can_tripplesex_dancegirls():
        if not (dis(dis_partyhouse) and school_dance_at_party()):
            
            return False
        elif t.wkday == "Sunday" and t.hour < 5:
            return True

    def can_tripplesex_soccergirls():
        if not loc(loc_school_field_back):
            
            return False
        elif not all([drake_here(), dan_here(), nate_here(), rachel_here(), robin_here()]):
            return False
        else:
            return True

    def can_tripplesex_pubgirls():
        if not dis(dis_pub):
            
            return False
        elif not all([dani_here(dis_pub), trixie_here(dis_pub)]):
            return False
        else:
            return True
# Decompiled by unrpyc: https://github.com/CensoredUsername/unrpyc
